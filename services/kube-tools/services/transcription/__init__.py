"""Transcription package — pipeline metadata.

Bump ``PIPELINE_VERSION`` manually whenever planner/scorer behaviour or
hysteresis thresholds change in a way that affects stored runs.
"""

PIPELINE_VERSION = "v2.chunkplanner.8"
